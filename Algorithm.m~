classdef Algorithm < handle
    properties
    
    end
    methods
        function predict(obj, data)
            structRes(1,1).hand = abs(data(1,1) - data(1,3));
            structRes(1,1).foot = abs(data(1,2) - data(1,4));
            if (structRes(1,1).hand < structRes(1,1).foot)
                structRes(1,1).effect = 'h';
            else
                structRes(1,1).effect = 'f';
            end
        end
    end
end